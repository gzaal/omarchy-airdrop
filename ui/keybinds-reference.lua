# Keybind suggestions for omarchy-airdrop (Hyprland)
# Add to ~/.config/hypr/bindings.lua using hl.bind, e.g.:
#
#   hl.bind({ "SUPER", "SHIFT" }, "A", function() o.spawn("omarchy-launch-floating-terminal airdrop receive --prompt") end)
#
# Suggested bindings:
#   SUPER+SHIFT+A  toggle receiver (terminal)
#   SUPER+ALT+A    share a file via airdrop-share
#
# This file is a reference only; install.sh prints instructions and does not
# edit your Hyprland config automatically.
